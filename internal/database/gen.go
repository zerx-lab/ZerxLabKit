package database

// GORM CLI query codegen directive. Prefer `task gen:db`, which runs the gorm
// CLI from the repo root: .bin/gorm gen -i ./internal/model -o ./internal/query
//
//go:generate gorm gen -i ../model -o ../query
